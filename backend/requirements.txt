#
# FLARE backend — Flask API + optional real MRI inference (predict_mri.py).
# Install: pip install -r requirements.txt
#
flask
flask-cors
numpy
opencv-python-headless
Pillow
torch
torchvision
segmentation-models-pytorch
# NIfTI (.nii / .nii.gz) → slice PNG for brain_mri only; see predict_mri._extract_best_slice_from_nifti
nibabel>=5.0.0

